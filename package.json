{
  "name": "note-hub",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client --include=dev",
    "prebuild": "npm install --prefix client --include=dev",
    "build": "npm run build --prefix client",
    "prestart": "npm install --prefix server",
    "start": "npm run start --prefix server"
  }
}
